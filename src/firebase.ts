/**
 * Firebase has been retired for this app.
 * Auth + data now run on Django REST API (src/lib/api.ts).
 * This file is kept empty on purpose so old imports fail loudly if reintroduced.
 */
export {};
